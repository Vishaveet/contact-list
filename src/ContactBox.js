function ContactBox() {
  return (
    <>
      <div className={isEdit ? "contact-box-new" : "conatct-box"}>
        
        {/* {(!isture && isEdit) ? <EditContact item={item.name}/>: ""} */}
      </div>
    </>
  );
}
export default ContactBox;
